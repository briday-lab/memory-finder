version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "🔄 Starting Amplify build process..."
        - echo "🧹 Clearing build cache..."
        - rm -rf .next node_modules/.cache
        - npm ci
        - echo "✅ Dependencies installed successfully"
    build:
      commands:
        - echo "🔨 Building Next.js application with cache cleared..."
        - echo "📋 Verifying file structure..."
        - ls -la src/components/ | head -10
        - ls -la src/lib/ | head -10
        - npm run build
        - echo "✅ Build completed successfully"
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*