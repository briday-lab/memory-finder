# AWS MediaConvert Configuration
# Add these to your .env.local file

# AWS Region
AWS_REGION=us-east-2

# S3 Buckets
S3_COMPILATIONS_BUCKET=memory-finder-compilations
S3_RAW_BUCKET=memory-finder-raw

# MediaConvert Service (uncomment when ready to use)
# MEDIACONVERT_ROLE_ARN=arn:aws:iam::YOUR_ACCOUNT:role/MediaConvertRole
# MEDIACONVERT_QUEUE_ARN=arn:aws:mediaconvert:us-east-2:YOUR_ACCOUNT:queues/Default

# Note: MediaConvert requires specific IAM roles and queues to be set up
# The system will fall back to simple compilation if MediaConvert is not configured
